/**
 * Версия и история — одним разбором на всё приложение.
 *
 * `CHANGELOG.md` попадает в бандл как текст и разбирается при загрузке модуля.
 * Раньше это делал экран настроек у себя, но история нужна ещё и проверке
 * обновлений — а разбирать один и тот же файл дважды значит однажды разойтись
 * в том, какая версия считается установленной.
 */

import changelogSource from '../../CHANGELOG.md?raw'
import { parseChangelog } from '../logic/changelog'

export const РЕЛИЗЫ = parseChangelog(changelogSource)

/** Установленная версия — верхняя запись истории, как и в «О приложении». */
export const ВЕРСИЯ = РЕЛИЗЫ[0]?.version ?? '0.0.0'
