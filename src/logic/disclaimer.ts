/**
 * Оговорки: чья это шкала и чей это совет.
 *
 * Приложение показывает категорию давления и оценку сахара, а при крайних
 * значениях — что при них обычно делают. И то и другое похоже на медицинское
 * суждение, а значит, должно быть подписано чужим именем там, где оно
 * показывается. Своей оценки у приложения нет: есть опубликованная шкала и
 * целевые значения, которые человек внёс со слов врача.
 *
 * **Оговорка не отменяет функции.** Если решать, быть приложению медизделием
 * или нет, — решает то, что оно делает, а не то, что о себе пишет
 * (письмо Росздравнадзора 02И-297/20, черта записана в `BACKLOG.md` §11).
 * Формулировка делает ровно три вещи, и все три полезны:
 *
 * 1. называет хозяина шкалы — это ESC/ESH, а не мы;
 * 2. называет хозяина совета — это общая рекомендация, а не назначение
 *    конкретному человеку;
 * 3. стоит рядом с оценкой, а не в углу «О приложении»: абзац в трёх касаниях
 *    от плашки «АГ 2 степени» не читает никто.
 *
 * Тексты живут здесь, а не по экранам, чтобы не разойтись между собой: три
 * разные формулировки одного обещания хуже, чем одна.
 */

/** Под категориями давления. */
export const SCALE_NOTE =
  'Категории — по шкале ESC/ESH, принятой в клинических рекомендациях. Это справочная разметка, а не диагноз.'

/** Под оценкой сахара: там сравнение идёт с целями самого человека. */
export const GLUCOSE_SCALE_NOTE =
  '«В норме» здесь значит «в пределах ваших целевых значений», а не общей нормы. Что значат сами цифры — вопрос к врачу.'

/** Под предупреждением о крайнем значении. */
export const ADVICE_NOTE =
  'Это общая рекомендация, а не назначение вам. Если врач назначил другой порядок действий — держитесь его.'

/** Назначение приложения — для «О приложении» и первого запуска. */
export const PURPOSE =
  'Приложение записывает измерения и приём лекарств, хранит их на этом телефоне и показывает врачу. ' +
  'Оно не ставит диагноз и не назначает лечение.'
